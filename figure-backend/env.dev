# Figure Backend Dev Environment (Cloud Services)
# 🌍 Supabase + Railway + Render 연동 환경

# App Configuration
FIGURE_APP_NAME=Figure Backend Dev
FIGURE_DEBUG=false
FIGURE_API_VERSION=v1
FIGURE_ENVIRONMENT=dev

# Server Configuration (Railway 자동 할당)
FIGURE_HOST=0.0.0.0
FIGURE_PORT=${PORT:-8001}

# Provider Configuration
FIGURE_LLM_PROVIDER=claude
FIGURE_EMBEDDING_PROVIDER=gemini

# API Keys (Railway Secrets로 관리)
FIGURE_CLAUDE_API_KEY=${CLAUDE_API_KEY}
FIGURE_GEMINI_API_KEY=${GEMINI_API_KEY}
FIGURE_OPENAI_API_KEY=${OPENAI_API_KEY:-}
FIGURE_VOYAGE_API_KEY=${VOYAGE_API_KEY:-}

# LLM Configuration
FIGURE_CLAUDE_MODEL=claude-3-5-sonnet-20241022
FIGURE_CLAUDE_MAX_TOKENS=4096
FIGURE_CLAUDE_TEMPERATURE=0.1

# Embedding Configuration  
FIGURE_GEMINI_MODEL=gemini-1.5-flash
FIGURE_GEMINI_EMBEDDING_MODEL=models/text-embedding-004
FIGURE_GEMINI_MAX_TOKENS=2048
FIGURE_GEMINI_TEMPERATURE=0.1

# 🐘 Supabase PostgreSQL Configuration
FIGURE_DATABASE_URL=postgresql://${SUPABASE_DB_USER}:${SUPABASE_DB_PASSWORD}@${SUPABASE_DB_HOST}:5432/${SUPABASE_DB_NAME}

# 🗂️ Supabase Storage Configuration
FIGURE_STORAGE_PROVIDER=supabase
FIGURE_SUPABASE_URL=${SUPABASE_URL}
FIGURE_SUPABASE_ANON_KEY=${SUPABASE_ANON_KEY}
FIGURE_SUPABASE_SERVICE_ROLE_KEY=${SUPABASE_SERVICE_ROLE_KEY}
FIGURE_SUPABASE_BUCKET_NAME=figure-documents

# 🎯 ChromaDB Configuration (Render)
FIGURE_CHROMA_URL=${CHROMA_URL}  # https://figure-chromadb.onrender.com
FIGURE_CHROMA_COLLECTION_NAME=figure_documents_dev

# 🔴 Redis Configuration (Railway 또는 Upstash)
FIGURE_REDIS_URL=${REDIS_URL}

# JIRA Configuration
FIGURE_JIRA_SERVER=${JIRA_SERVER:-}
FIGURE_JIRA_EMAIL=${JIRA_EMAIL:-}
FIGURE_JIRA_API_TOKEN=${JIRA_API_TOKEN:-}

# Security
FIGURE_JWT_SECRET=${JWT_SECRET}
FIGURE_CORS_ORIGINS=["https://figure-office.vercel.app","http://localhost:3001"]

# Logging
FIGURE_LOG_LEVEL=info
FIGURE_LOG_FILE=/tmp/figure.log

# SSL 및 보안 설정
PYTHONHTTPSVERIFY=1
FIGURE_SSL_VERIFY=true

# Performance
FIGURE_MAX_WORKERS=2
FIGURE_TIMEOUT=30
FIGURE_KEEPALIVE=2
