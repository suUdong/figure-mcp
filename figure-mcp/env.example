# Figure MCP Server 환경 변수 설정

# 백엔드 API 설정 (선택사항)
# 설정하지 않으면 자동으로 환경을 감지합니다:
# - 로컬 개발: http://localhost:8001/api
# - Docker 환경: http://figure-backend:8001/api
# BACKEND_API_URL=http://localhost:8001/api

# Docker 환경 감지 (선택사항)
# Docker 환경에서 실행 중임을 명시적으로 설정
# DOCKER_ENV=true
# NODE_ENV=docker

# 🏢 기본 사이트 설정 (중요!)
# 템플릿은 사이트별로 관리되므로 기본 사이트 설정이 필요합니다.
# MCP에서 siteName을 생략할 때 이 설정이 사용됩니다.
# 백오피스(http://localhost:3001)에서 사이트 ID 또는 이름을 확인하세요.
# 
# ⚡ 둘 중 하나만 설정하면 됩니다:
# DEFAULT_SITE_ID=your-site-id        # 사이트 ID로 설정하거나
DEFAULT_SITE_NAME=KT알파               # 사이트 이름으로 설정하거나

# JIRA 연동 설정 (선택사항)
JIRA_BASE_URL=https://your-domain.atlassian.net
JIRA_EMAIL=your-email@company.com
JIRA_API_TOKEN=your-jira-api-token

# 데이터베이스 연결 설정 (테이블 명세서 자동 생성용, 선택사항)
# 설정하지 않으면 수동 입력 모드로 동작합니다.
DB_CONNECTION_STRING=mysql://username:password@localhost:3306/database_name
DB_TYPE=mysql

# 🚀 설정 가이드:
# 
# 1. 🏢 기본 사이트 설정 (필수):
#    - 백오피스 관리자 UI (http://localhost:3001)에 접속
#    - 사이트 관리에서 사이트 ID 또는 이름 확인  
#    - DEFAULT_SITE_ID 또는 DEFAULT_SITE_NAME 중 하나만 설정 ⚡
#    - 설정 후 MCP에서 siteName 생략 가능! 🎯
#
# 2. JIRA API Token 생성:
#    - JIRA 계정 설정 → 보안 → API 토큰 생성
#    - https://id.atlassian.com/manage-profile/security/api-tokens
#    - 생성된 토큰을 JIRA_API_TOKEN에 입력
#
# 3. 데이터베이스 설정 (테이블 명세서 자동화):
#    - DB_CONNECTION_STRING: 데이터베이스 연결 문자열
#      * MySQL: mysql://user:password@host:port/database
#      * PostgreSQL: postgresql://user:password@host:port/database
#      * SQLite: sqlite:///path/to/database.db
#    - DB_TYPE: 데이터베이스 종류 (mysql, postgresql, oracle, mssql, sqlite)
#    - 설정하지 않으면 테이블 명세서 생성 시 수동 입력 모드로 동작
